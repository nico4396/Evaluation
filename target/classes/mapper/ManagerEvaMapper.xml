<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- namespace目前可以自定义要唯一，将来面向接口编程时对应dao接口，一 个mapper相当于一个dao接口的实现类 -->
<mapper namespace="com.jxd.dao.IManagerEvaDao">
    <select id="getAllStudent" resultType="Student">
        select s.sid,s.sname,s.sex,s.school,s.place,tw.tscore
        from student s
        left join turnwork tw
        on s.sid=tw.sid
        <where>
            <if test="sname!=null and sname!=''">
                sname like '%' #{sname} '%'
            </if>
            <if test="jid!=null ">
                /*jid like '%' #{jid} '%'*/
                and jid=#{jid}
            </if>
        </where>
    </select>

    <select id="getStudentByPage" resultType="Student">
        select s.sid,s.sname,s.sex,s.school,s.place,tw.tscore
        from student s
        left join turnwork tw
        on s.sid=tw.sid
        <where>
            <if test="sname!=null and sname!=''">
                sname like '%' #{sname} '%'
            </if>
            <if test="jid!=null and sname!=''">
                /*jid like '%' #{jid} '%'*/
                jid=#{jid}
            </if>
        </where>
        limit #{pageSize},#{page}
    </select>

    <select id="getClassesName" resultType="Classes">
        select classname from classes
    </select>

    <insert id="evaTurnWork">
        insert into turnwork(appraise,tscore,content)
        values (#{appraise},#{score},#{content})
    </insert>
</mapper>